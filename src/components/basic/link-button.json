{
  "collectionName": "components_basic_link_buttons",
  "info": {
    "displayName": "Link/button",
    "icon": "link"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "link",
        "filled",
        "outline"
      ]
    },
    "target": {
      "type": "enumeration",
      "required": true,
      "default": "_self",
      "enum": [
        "_self",
        "_blank"
      ]
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "mint",
        "carbon",
        "cherry",
        "ash"
      ]
    },
    "size": {
      "type": "enumeration",
      "enum": [
        "default",
        "small",
        "large"
      ]
    },
    "URL": {
      "type": "string"
    }
  },
  "config": {}
}
